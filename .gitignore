node_modules/
dist/
coverage/

# Stray lockfile created when a command accidentally runs from the repo
# root instead of backend/ or frontend/ — there's no root package.json.
/package-lock.json
.env
.env.local
*.log

# Expo
.expo/
.expo-shared/

# macOS / editors / Windows
.DS_Store
Thumbs.db
.vscode/
.claude/

# Raw sprite-generator output for character art (design/) and personal
# scratch files like CSV exports (local/) — source material, never shipped.
# The parts actually used by the app are copied into frontend/assets/.
/design/
/local/
